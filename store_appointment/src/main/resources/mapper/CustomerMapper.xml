<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wkr.store_appointment.mapper.CustomerMapper">

    <select id="page" resultType="com.wkr.store_appointment.pojo.vo.CustomerVO">
        select * from customer
        <where>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
            <if test="phone != null and phone != ''">
                and phone like concat('%', #{phone}, '%')
            </if>
            <if test="level != null and level != ''">
                and level = #{level}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="listForExport" resultType="com.wkr.store_appointment.pojo.vo.CustomerVO">
        select * from customer
        <where>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
            <if test="phone != null and phone != ''">
                and phone like concat('%', #{phone}, '%')
            </if>
            <if test="level != null and level != ''">
                and level = #{level}
            </if>
        </where>
        order by create_time desc
    </select>
</mapper>
